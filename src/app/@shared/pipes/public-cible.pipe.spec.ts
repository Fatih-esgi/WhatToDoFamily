import { PublicCiblePipe } from './public-cible.pipe';

describe('PublicCiblePipe', () => {
  it('create an instance', () => {
    const pipe = new PublicCiblePipe();
    expect(pipe).toBeTruthy();
  });
});
