import { IconHandicapPipe } from './icon-handicap.pipe';

describe('IconHandicapPipe', () => {
  it('create an instance', () => {
    const pipe = new IconHandicapPipe();
    expect(pipe).toBeTruthy();
  });
});
