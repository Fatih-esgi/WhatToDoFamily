import { CatColorsPipe } from './cat-colors.pipe';

describe('CatColorsPipe', () => {
  it('create an instance', () => {
    const pipe = new CatColorsPipe();
    expect(pipe).toBeTruthy();
  });
});
