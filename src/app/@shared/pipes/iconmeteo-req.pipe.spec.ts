import { IconmeteoReqPipe } from './iconmeteo-req.pipe';

describe('IconmeteoReqPipe', () => {
  it('create an instance', () => {
    const pipe = new IconmeteoReqPipe();
    expect(pipe).toBeTruthy();
  });
});
