import { ToolbarEffectDirective } from './toolbar-effect.directive';

describe('ToolbarEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new ToolbarEffectDirective();
    expect(directive).toBeTruthy();
  });
});
